# Most Tower modules are still empty skeletons. A boundary rule that currently
# matches no classes is not a failure — it is a constraint waiting for the code
# it governs. Locking the boundaries before the code arrives is the point.
archRule.failOnEmptyShould=false
