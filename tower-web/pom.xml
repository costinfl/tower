<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.tower</groupId>
    <artifactId>tower-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>tower-web</artifactId>
  <name>tower-web</name>

  <!--
    The Viewer. Communicates exclusively with tower-api and never with an
    External System (CRC-Cards.md, Viewer responsibilities).

    This module has no Java sources. frontend-maven-plugin installs a
    project-local Node/npm, runs "npm ci" and "npm run build" against the
    React + TypeScript + Vite application under src/, and maven-resources-plugin
    copies the resulting dist/ into META-INF/resources so Spring Boot serves the
    built SPA as static content. The net effect is that a single
    `mvn spring-boot:run` (from tower-api, which depends on this jar) serves
    both the API and the UI with no separate frontend step (ADR-009,
    clone-build-run).
  -->

  <build>
    <plugins>
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <!--
          The downloaded Node lives under target/ rather than beside the sources,
          because it is a build artifact and not part of the project.

          That placement is also what makes a Node version bump safe. The plugin
          upgrades an existing installation in place - it swaps the node binary
          and re-extracts npm over whatever is already there - and the result is
          a node and an npm that do not match, which fails with "Class extends
          value undefined is not a constructor or null". Nothing in that message
          suggests a stale toolchain, and the build that produced it had passed
          on the machine where the directory happened to be fresh.

          Under target/ the toolchain is disposable: `mvn clean` removes it, CI
          starts without it, and anyone whose checkout predates a version bump
          gets a correct install rather than an upgraded one. Raising
          node.version can no longer break a build that a clean would fix.
        -->
        <configuration>
          <installDirectory>${project.build.directory}</installDirectory>
        </configuration>
        <executions>
          <execution>
            <id>install-node-and-npm</id>
            <goals>
              <goal>install-node-and-npm</goal>
            </goals>
            <configuration>
              <nodeVersion>${node.version}</nodeVersion>
            </configuration>
          </execution>
          <execution>
            <id>npm-install</id>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>ci</arguments>
            </configuration>
          </execution>
          <execution>
            <id>npm-build</id>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>run build</arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-frontend-dist</id>
            <!--
              generate-resources (frontend-maven-plugin's default phase for the
              executions above) runs before process-resources, so dist/ exists
              by the time this copy runs.
            -->
            <phase>process-resources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.outputDirectory}/META-INF/resources</outputDirectory>
              <resources>
                <resource>
                  <directory>${project.basedir}/dist</directory>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
